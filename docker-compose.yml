version: "3"

services:
  web:
    build: ./server
    container_name: challenge-energy-simply-web
    image: challenge-energy-simply:0.5.0
    ports:
      - "80:80"
    depends_on:
            - db
  db:
    image: "microsoft/mssql-server-linux"
    environment:
      SA_PASSWORD: "P@ssw0rd"
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"